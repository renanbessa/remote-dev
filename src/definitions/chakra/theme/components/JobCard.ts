import {ComponentStyleConfig} from '@chakra-ui/theme'

export const JobCard: ComponentStyleConfig = Object.freeze({
  baseStyle: {
    px: 5,
    py: 7,
    bg: 'yellow.100',
    borderRadius: 8,
    gap: 8,
  },
})
